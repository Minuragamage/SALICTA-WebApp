export enum RouteName {
  ROOT = "/",
  MAIN = "/main",
  LOGIN = "/login",
  CONTACT = "/contact",
  CART = "/cart",
  CHECKOUT = "/checkout",
  PRODUCTS = "/products",
  ADD_PRODUCTS = "/add-products",
  ORDERS = "/orders",
  ABOUT = "/about",
  DASHBOARD = "/dashboard",
}
